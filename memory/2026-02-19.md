
- Dropshipping運用方針を更新：在庫先仕入れ前提のカテゴリではなく、**dropshipping向けカテゴリで再選定**する方針に変更。
- 初期カテゴリは **ホーム&キッチンの消耗/補助小物** で進行することで合意。
- Hikaruの希望：実装と運用は可能な限り自動で継続し、**止まる/課金が発生するタイミングのみ相談**する。
- 呼称/文体の明確化：アシスタントの一人称は「俺」ではなく **「小生」** を使用する。
- kando1-lp に eBay Marketplace Account Deletion 通知用エンドポイントを追加済み（`/api/ebay/account-deletion`）。
  - eBay検証で失敗しやすいポイントは endpoint の完全一致（特に `www` 有無）と verification token バリデーション。
- eBay Production keyset有効化対応を進行。Marketplace Account Deletion通知の検証失敗要因を修正：
  - `/api/ebay/account-deletion` で `GET ?challenge_code=` に対して `sha256(challengeCode + verificationToken + endpoint)` を返す実装へ変更。
  - `kando1.com`→`www.kando1.com` リダイレクト差分が検証失敗要因になりうるため、endpointは `https://www.kando1.com/api/ebay/account-deletion` で統一する方針。
- eBay UIの Save不可は verification token バリデーション（文字種/長さ/不可視文字混入）で発生しやすいことを確認。再ログインで入力UI不具合は解消。
