# 2026-02-26

## 今日の実施ログ（要約）

- 文言修正（kando1-lp）
  - `最短1〜2週間 / 簡易コーポレートLP制作向け` に統一。
- kando1-lp のSEO/計測対応
  - `public/robots.txt` 作成
  - `public/llms.txt` 作成
  - `app/sitemap.ts` 作成
  - GA4実装（`NEXT_PUBLIC_GA_ID`を読む形で`layout.tsx`にタグ追加）
  - Header/Footer の内部リンクを `<a>` から `next/link` に置換してlint通過
  - featureブランチから`main`へマージ・push完了
- mei-tra-frontend のGA4対応
  - `app/layout.tsx` に GA4 実装（`NEXT_PUBLIC_GA_ID`方式）
  - `.env.local` に `NEXT_PUBLIC_GA_ID=G-RLDTECZBC3` 追記
  - `mei-tra` mainへrebase後push完了
- Codex設定修正
  - `~/.codex/agents/custom-explorer.toml` 作成
  - `~/.codex/agents/reviewer.toml` 作成
  - `config_file`参照エラーの原因を解消
- polymarket-bot 強化
  - P0: EV/Kelly sizing、execution policy（cooldown/max trades）実装
  - P1: signal aggregator、WS price feed、校正付き`p_true`、Discord daily summary追加
  - `main`へpush済み
  - fastloop未コミット差分も確認し、問題ないため追加で`main`へpush済み
- Shopifyアプリ開発着手
  - 新規repo `kando1-ec-shopify` 作成・初期push
  - `feature/mvp-bulk-pricing` でMVP API土台実装
    - `/api/simulate` `/api/apply` `/api/jobs/:id` `/api/jobs/:id/undo`
  - `node_modules`誤コミットを即修正（.gitignore追加、履歴上は除去コミット済）

## 重要メモ

- relay/browser snapshot が断続的にタイムアウト。tab一覧は取れるが snapshot が不安定。
- Shopify検証は「Paint Test商品」「Bulk edit」まで進捗確認済み。
- ユーザー意図は「ドロップシッピング販売そのもの」より「Shopifyアプリ市場調査とMVP実装」。

## 引き継ぎ3行

1. 今日やったこと
   - kando1-lp/mei-traのGA・SEO対応、polymarket-botのP0/P1実装、Shopify新規repoでMVP API土台まで実装。
2. 現在状態（数値）
   - kando1-lp main反映済み、mei-tra main反映済み、polymarket-bot main反映済み、kando1-ec-shopifyは`feature/mvp-bulk-pricing`に実装コミット2件。
3. 次にやる1手
   - `kando1-ec-shopify` の Rule Builder + Preview UI（埋め込み前提の最小画面）を実装して、Shopify実データ接続へ進む。
